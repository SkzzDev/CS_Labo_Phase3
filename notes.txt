Questions:

	Type type = typeof(User);
	// Comment créer une liste du type de la variable type ?

	string[] UniquesFields dans les classes héritant de IXMLSerializable, quel meilleur équivalent à une variable membre static dans une interface?

	Héritage d'une const (dans Model et ses sous models) ?
